const Footer = ({ className }: { className?: string }) => {
  return (
    <footer className={`bg-sky-800 text-sky-100 ${className}`}>
      Some kind of footer
    </footer>
  );
};
export default Footer;
